sudo: false

language: cpp

cache:
  apt: true

addons:
  apt:
    sources:
      - boost-latest
    packages:
      - libboost-all-dev

compiler:
  - gcc
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ../

script:
  - make